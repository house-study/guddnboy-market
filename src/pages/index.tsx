import Layout from '@/components/layout';
import ProductList from '@/components/ProductList';

const Home = () => {
  return (
    <Layout>
      <ProductList />
    </Layout>
  );
};

export default Home;
